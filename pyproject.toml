[project]
name = "gym-atari"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ale-py>=0.10.1",
    "gymnasium[other]>=1.0.0",
    "torch==2.5.1",
    "requests>=2.32.3",
]

#  don't forget to install cuda when needed